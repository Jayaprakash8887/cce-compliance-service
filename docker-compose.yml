# ==============================================================================
# CCE Compliance Service — Docker Compose
# ==============================================================================
# PostgreSQL, Kafka, and the shared database (cce_collector) are deployed by the
# CCE Collector Service. Start collector infrastructure first:
#   cd /path/to/cce-collector-service && docker compose up -d
#
# All CCE services share the same database. Flyway applies compliance service
# schema migrations automatically on application startup.
# ==============================================================================

# No services needed — infrastructure is provided by cce-collector-service.
# This file is kept as a placeholder for future service-specific containers.
